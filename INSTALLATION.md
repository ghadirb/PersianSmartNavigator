# 🚀 نصب و راه‌اندازی مسیریاب هوشمند فارسی

## 📁 پروژه:
```
C:\Users\Admin\CascadeProjects\PersianSmartNavigator
```

## ✅ ماژول‌های هوش مصنوعی:

### 1. KeyManager.kt
- مدیریت کلیدهای API رمزشده
- لینک: https://drive.google.com/file/d/17iwkjyGcxJeDgwQWEcsOdfbOxOah_0u0

### 2. RouteAnalyzer.kt
- تحلیل هوشمند مسیر و ترافیک

### 3. PersianVoiceAlerts.kt
- هشدارهای صوتی فارسی

### 4. RouteLearning.kt
- یادگیری مسیرهای پرتکرار
- بک‌آپ به: https://drive.google.com/drive/folders/1bp1Ay9kmK_bjWq_PznRfkPvhhjdhSye1

### 5. ChatFragment.kt
- چت با مدل AI

## 🔧 نصب:

### 1. Clone پروژه:
```bash
git clone https://github.com/ghadirb/PersianSmartNavigator.git
cd PersianSmartNavigator
```

### 2. Build:
```bash
./gradlew assembleDebug
```

### 3. نصب APK:
```bash
adb install OsmAnd/build/outputs/apk/debug/OsmAnd-debug.apk
```

## 🎯 استفاده:

### فعال‌سازی کلیدهای API:
1. باز کردن برنامه
2. رفتن به تنظیمات
3. وارد کردن پسورد رمزگشایی
4. کلیدها خودکار دانلود و فعال می‌شوند

### استفاده از چت AI:
1. کلیک روی آیکون چت
2. پرسیدن سوال
3. دریافت پاسخ هوشمند

### هشدارهای صوتی:
- خودکار فعال است
- هشدار سرعت
- هشدار دوربین
- راهنمای مسیر

## 📊 امکانات:

```
✅ مسیریابی آفلاین (OsmAnd)
✅ نقشه‌های کامل
✅ تحلیل هوشمند مسیر
✅ هشدارهای صوتی فارسی
✅ یادگیری مسیر
✅ چت AI
✅ بک‌آپ خودکار
✅ رمزنگاری کلیدها
```

## 🔐 امنیت:

- کلیدهای API رمزشده
- PBKDF2 + AES-GCM
- بک‌آپ امن به Google Drive

## 📝 توسعه:

### اضافه کردن ماژول جدید:
```kotlin
// در OsmAnd/src/net/osmand/plus/ai/
package net.osmand.plus.ai

class YourModule(context: Context) {
    // کد شما
}
```

### استفاده از KeyManager:
```kotlin
val keyManager = KeyManager(context)
keyManager.unlockKeys("password")
val apiKey = keyManager.getCurrentKey()
```

## ⚠️ نکات:

1. **حجم بزرگ**: پروژه حدود 88 MB است
2. **Build اولیه**: ممکن است 10-15 دقیقه طول بکشد
3. **نیاز به اینترنت**: برای دانلود کلیدها و بک‌آپ

## 🆘 مشکلات رایج:

### Build خطا می‌دهد:
```bash
./gradlew clean
./gradlew assembleDebug
```

### کلیدها دانلود نمی‌شوند:
- بررسی اتصال اینترنت
- بررسی لینک Google Drive

### صدای فارسی کار نمی‌کند:
- نصب TTS فارسی از Google Play
- فعال‌سازی در تنظیمات

---

**پروژه آماده است! 🎉**
